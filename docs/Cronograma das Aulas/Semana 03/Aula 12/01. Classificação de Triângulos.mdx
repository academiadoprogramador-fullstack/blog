## Introdução

Dados três valores X, Y, Z, verifiquem se eles podem ser os comprimentos dos lados de um triângulo e se forem escrever uma mensagem informando se é um triângulo eqüilátero, isósceles ou escaleno.

**Observações:**
- O comprimento de um lado do triângulo é sempre menor do que a soma dos outros dois.

**Classificações:**
- Eqüilátero: Todos os lados iguais
- Isósceles: Dois lados iguais
- Escaleno: Todos os lados diferentes

## Passo a passo

- Entrada de Dados Básica
- Verificação de Triângulo Válido
- Classificação do Triângulo
- Tratamento de Entradas Inválidas
- Melhoras na legibilidade do código
- Utilizar classe estática ClassificadorDeTriangulos

## Versão 1 - Entrada de Dados Básica

 - O sistema deve apresentar uma mensagem de boas-vindas
 - O sistema deve solicitar os três valores correspondentes aos lados do triângulo
 - O sistema deve exibir os valores digitados para confirmação
 - O sistema deve encerrar após mostrar os valores

```cs
static void Main(string[] args)
{
    while (true)
    {
        Console.Clear();
		Console.WriteLine("-------------------------------------------------");
        Console.WriteLine("Classificador de Triângulos");
        Console.WriteLine("-------------------------------------------------");
        
		Console.Write("Informe o valor do lado X: ");
		int ladoX = Convert.ToInt32(Console.ReadLine());

		Console.Write("Informe o valor do lado Y: ");
		int ladoY = Convert.ToInt32(Console.ReadLine());

		Console.Write("Informe o valor do lado Z: ");
		int ladoZ = Convert.ToInt32(Console.ReadLine());

        Console.WriteLine("-------------------------------------------------");
        Console.WriteLine("Valores informados:");
        Console.WriteLine($"Lado X: {ladoX}");
        Console.WriteLine($"Lado Y: {ladoY}");
        Console.WriteLine($"Lado Z: {ladoZ}");
        Console.WriteLine("-------------------------------------------------");

        Console.WriteLine("-------------------------------------------------");
        Console.Write("Deseja continuar? (s/N) ");

        string opcaoContinuar = Console.ReadLine()!.ToUpper();

        if (opcaoContinuar != "S")
            break;
    }
}
```
`Program.cs`
## Versão 2 - Verificação de Triângulo Válido

 - O sistema deve manter todos os requisitos da Versão 1
 - O sistema deve verificar se os três valores formam um triângulo válido
 - O sistema deve informar ao usuário se os valores formam ou não um triângulo válido

```cs
while (true)
{
    Console.Clear();
    Console.WriteLine("-------------------------------------------------");
    Console.WriteLine("Classificador de Triângulos");
    Console.WriteLine("-------------------------------------------------");

    
	Console.Write("Informe o valor do lado X: ");
	int ladoX = Convert.ToInt32(Console.ReadLine());

	Console.Write("Informe o valor do lado Y: ");
	int ladoY = Convert.ToInt32(Console.ReadLine());

	Console.Write("Informe o valor do lado Z: ");
	int ladoZ = Convert.ToInt32(Console.ReadLine());

    Console.WriteLine("-------------------------------------------------");
    Console.WriteLine("Valores informados:");
    Console.WriteLine($"Lado X: {ladoX}");
    Console.WriteLine($"Lado Y: {ladoY}");
    Console.WriteLine($"Lado Z: {ladoZ}");
    Console.WriteLine("-------------------------------------------------");

    bool medidasFormamTrianguloValido = 
        ladoX + ladoY > ladoZ &&
        ladoX + ladoZ > ladoY &&
        ladoY + ladoZ > ladoX;

    if (medidasFormamTrianguloValido)
        Console.WriteLine("O triângulo é valido.");
    else
        Console.WriteLine("Os valores informados não formam um triângulo válido.");

    Console.WriteLine("-------------------------------------------------");
    Console.Write("Deseja continuar? (s/N) ");

    string opcaoContinuar = Console.ReadLine()!.ToUpper();

    if (opcaoContinuar != "S")
        break;
}
```
`Program.cs`
## Versão 3 - Classificação do Triângulo

 * O sistema deve manter todos os requisitos da Versão 2
 * O sistema deve classificar o triângulo como Equilátero, Isósceles ou Escaleno, caso seja válido

```cs
while (true)
{
    Console.Clear();
    Console.WriteLine("-------------------------------------------------");
    Console.WriteLine("Classificador de Triângulos");
    Console.WriteLine("-------------------------------------------------");

    Console.Write("Informe o valor do lado X: ");
    int ladoX = Convert.ToInt32(Console.ReadLine());

    Console.Write("Informe o valor do lado Y: ");
    int ladoY = Convert.ToInt32(Console.ReadLine());

    Console.Write("Informe o valor do lado Z: ");
    int ladoZ = Convert.ToInt32(Console.ReadLine());

    Console.WriteLine("-------------------------------------------------");
    Console.WriteLine("Valores informados:");
    Console.WriteLine($"Lado X: {ladoX}");
    Console.WriteLine($"Lado Y: {ladoY}");
    Console.WriteLine($"Lado Z: {ladoZ}");
    Console.WriteLine("-------------------------------------------------");

    bool medidasFormamTrianguloValido = 
        ladoX + ladoY > ladoZ &&
        ladoX + ladoZ > ladoY &&
        ladoY + ladoZ > ladoX;

    if (medidasFormamTrianguloValido)
    {
        string tipoTriangulo = "Não classificado";

        if (ladoX == ladoY && ladoY == ladoZ)
            tipoTriangulo = "Equilátero";
        else if (ladoX != ladoY && ladoY != ladoZ && ladoX != ladoZ)
            tipoTriangulo = "Escaleno";
        else
            tipoTriangulo = "Isósceles";

        Console.WriteLine($"O triângulo é {tipoTriangulo}.");
    }
    else
        Console.WriteLine("Os valores informados não formam um triângulo válido.");

    Console.WriteLine("-------------------------------------------------");
    Console.Write("Deseja continuar? (s/N) ");

    string opcaoContinuar = Console.ReadLine()!.ToUpper();

    if (opcaoContinuar != "S")
        break;
}
```
`Program.cs`

## Versão 4 - Tratamento de Entradas Inválidas

 * O sistema deve manter todos os requisitos da Versão 3
 * O sistema deve tratar erros de digitação (valores não numéricos)
 * O sistema deve verificar se os valores digitados são positivos 

```cs
while (true)
{
    Console.Clear();
    Console.WriteLine("-------------------------------------------------");
    Console.WriteLine("Classificador de Triângulos");
    Console.WriteLine("-------------------------------------------------");

    int ladoX = 0;

    while (true)
    {
        Console.Write($"Informe o valor do lado X: ");

        bool conseguiuConverter = int.TryParse(Console.ReadLine(), out ladoX);

        if (conseguiuConverter && ladoX > 0)
            break;

	    Console.WriteLine("-------------------------------------------------");
        Console.WriteLine("Entrada inválida! Informe apenas números inteiros positivos.");
	    Console.WriteLine("-------------------------------------------------");
    }

    int ladoY = 0;

    while (true)
    {
        Console.Write($"Informe o valor do lado Y: ");

        bool conseguiuConverter = int.TryParse(Console.ReadLine(), out ladoY);

        if (conseguiuConverter && ladoY > 0)
            break;

	    Console.WriteLine("-------------------------------------------------");
        Console.WriteLine("Entrada inválida! Informe apenas números inteiros positivos.");
	    Console.WriteLine("-------------------------------------------------");
    }

    int ladoZ = 0;

    while (true)
    {
        Console.Write($"Informe o valor do lado Z: ");

        bool conseguiuConverter = int.TryParse(Console.ReadLine(), out ladoZ);

        if (conseguiuConverter && ladoZ > 0)
            break;

	    Console.WriteLine("-------------------------------------------------");
        Console.WriteLine("Entrada inválida! Informe apenas números inteiros positivos.");
		Console.WriteLine("-------------------------------------------------");
    }

    Console.WriteLine("-------------------------------------------------");
    Console.WriteLine("Valores informados:");
    Console.WriteLine($"Lado X: {ladoX}");
    Console.WriteLine($"Lado Y: {ladoY}");
    Console.WriteLine($"Lado Z: {ladoZ}");
    Console.WriteLine("-------------------------------------------------");

    bool medidasFormamTrianguloValido = 
        ladoX + ladoY > ladoZ &&
        ladoX + ladoZ > ladoY &&
        ladoY + ladoZ > ladoX;

    if (medidasFormamTrianguloValido)
    {
        string tipoTriangulo = "Não classificado";

        if (ladoX == ladoY && ladoY == ladoZ)
            tipoTriangulo = "Equilátero";
        else if (ladoX != ladoY && ladoY != ladoZ && ladoX != ladoZ)
            tipoTriangulo = "Escaleno";
        else
            tipoTriangulo = "Isósceles";

        Console.WriteLine($"O triângulo é {tipoTriangulo}.");
    }
    else
        Console.WriteLine("Os valores informados não formam um triângulo válido.");

    Console.WriteLine("-------------------------------------------------");
    Console.Write("Deseja continuar? (s/N) ");

    string opcaoContinuar = Console.ReadLine()!.ToUpper();

    if (opcaoContinuar != "S")
        break;
}
```

## Versão 5 - Melhoras na legibilidade do código

 * Reutilizar subrotinas (métodos) removendo códigos duplicados
 * Melhorar a legibilidade do código 

```cs
internal class Program
{
    static void Main()
    {
        while (true)
        {
            ExibirCabecalho();
            int ladoX = LerLado("X");
            int ladoY = LerLado("Y");
            int ladoZ = LerLado("Z");

            ExibirValoresInformados(ladoX, ladoY, ladoZ);

            if (MedidasFormamTrianguloValido(ladoX, ladoY, ladoZ))
            {
                string classificacaoTriangulo = ClassificarTriangulo(ladoX, ladoY, ladoZ);

                Console.WriteLine($"O triângulo é {classificacaoTriangulo}.");
            }
            else
                Console.WriteLine("Os valores informados não formam um triângulo válido.");

            string opcaoContinuar = ExibirMenuOpcaoContinuar();

            if (opcaoContinuar != "S")
                break;
        }
    }

    static void ExibirCabecalho()
    {
        Console.Clear();
	    Console.WriteLine("-------------------------------------------------");
        Console.WriteLine("Classificador de Triângulos");
	    Console.WriteLine("-------------------------------------------------");
    }

    static int LerLado(string nomeLado)
    {
        while (true)
        {
            Console.Write($"Informe o lado {nomeLado}: ");

            bool conseguiuConverter = int.TryParse(Console.ReadLine(), out int valor);

            if (conseguiuConverter && valor > 0)
                return valor;

		    Console.WriteLine("-------------------------------------------------");
            Console.WriteLine("Entrada inválida! Informe apenas números inteiros positivos.");
		    Console.WriteLine("-------------------------------------------------");
        }
    }

    static void ExibirValoresInformados(int ladoX, int ladoY, int ladoZ)
    {
	    Console.WriteLine("-------------------------------------------------");
        Console.WriteLine("Valores informados:");
        Console.WriteLine($"Lado X: {ladoX}");
        Console.WriteLine($"Lado Y: {ladoY}");
        Console.WriteLine($"Lado Z: {ladoZ}");
	    Console.WriteLine("-------------------------------------------------");
    }
    
    static bool MedidasFormamTrianguloValido(int x, int y, int z)
    {
        return x + y > z && x + z > y && y + z > x;
    }
    
    static string ClassificarTriangulo(int x, int y, int z)
    {
        if (x == y && y == z)
            return "Equilátero";
        else if (x != y && y != z && x != z)
            return "Escaleno";
        else
            return "Isósceles";
    }

    static string ExibirMenuOpcaoContinuar()
    {
	    Console.WriteLine("-------------------------------------------------");
        Console.Write("Deseja continuar? (s/N) ");

        string opcaoContinuar = Console.ReadLine()!.ToUpper();

        return opcaoContinuar;
    }
}
```
`Program.cs`

## Versão 6 - Utilizar classe estática ClassificadorDeTriangulos

 * Utilizar classe estática para separar responsabilidades dos métodos
 * Melhorar a legibilidade do código 

```cs
public class ClassificadorDeTriangulos
{
    public static bool MedidasFormamTrianguloValido(int x, int y, int z)
    {
        return x + y > z && x + z > y && y + z > x;
    }

    public static string ClassificarTriangulo(int x, int y, int z)
    {
        if (x == y && y == z)
            return "Equilátero";
        else if (x != y && y != z && x != z)
            return "Escaleno";
        else
            return "Isósceles";
    }
}
```
`ClassificadorDeTriangulos.cs`

```cs
internal class Program
{
    static void Main()
    {
        while (true)
        {
            ExibirCabecalho();
            int ladoX = LerLado("X");
            int ladoY = LerLado("Y");
            int ladoZ = LerLado("Z");

            ExibirValoresInformados(ladoX, ladoY, ladoZ);

            bool medidasFormamTrianguloValido =
                ClassificadorDeTriangulos.MedidasFormamTrianguloValido(ladoX, ladoY, ladoZ);

            if (medidasFormamTrianguloValido)
            {
                string classificacaoTriangulo = 
                    ClassificadorDeTriangulos.ClassificarTriangulo(ladoX, ladoY, ladoZ);

                Console.WriteLine($"O triângulo é {classificacaoTriangulo}.");
            }
            else
                Console.WriteLine("Os valores informados não formam um triângulo válido.");

            string opcaoContinuar = ExibirMenuOpcaoContinuar();

            if (opcaoContinuar != "S")
                break;
        }
    }

    static void ExibirCabecalho()
    {
        Console.Clear();
	    Console.WriteLine("-------------------------------------------------");
        Console.WriteLine("Classificador de Triângulos");
	    Console.WriteLine("-------------------------------------------------");
    }

    static int LerLado(string nomeLado)
    {
        while (true)
        {
            Console.Write($"Informe o lado {nomeLado}: ");

            bool conseguiuConverter = int.TryParse(Console.ReadLine(), out int valor);

            if (conseguiuConverter && valor > 0)
                return valor;

		    Console.WriteLine("-------------------------------------------------");
            Console.WriteLine("Entrada inválida! Informe apenas números inteiros positivos.");
		    Console.WriteLine("-------------------------------------------------");
        }
    }

    static void ExibirValoresInformados(int ladoX, int ladoY, int ladoZ)
    {
	    Console.WriteLine("-------------------------------------------------");
        Console.WriteLine("Valores informados:");
        Console.WriteLine($"Lado X: {ladoX}");
        Console.WriteLine($"Lado Y: {ladoY}");
        Console.WriteLine($"Lado Z: {ladoZ}");
	    Console.WriteLine("-------------------------------------------------");
    }

    static string ExibirMenuOpcaoContinuar()
    {
	    Console.WriteLine("-------------------------------------------------");
        Console.Write("Deseja continuar? (s/N) ");

        string opcaoContinuar = Console.ReadLine()!.ToUpper();

        return opcaoContinuar;
    }
}
```
`Program.cs`